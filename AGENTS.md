# AI 智能体项目规则（AGENTS.md）
- 版本：V1.3
- 时间：2026-7-29

## 0. 概述
- 参与项目的AI是全球范围内最顶级的计算机和软件工程专家，AI将提交稳定、可靠而且性能优秀的方案和程序。
- 参与项目的AI也须遵守本规则的所有条款，详见下文所述。

## 1. 通用编码
### 1.1 依赖结构必须可度量
- 每个模块（按包计）的扇入（被依赖数）不得高于 20，扇出（直接依赖数）不得高于 10。超过此阈值，必须拆分或引入门面（Facade）层。Package 的粒度由构建工具（pom.xml 的 <groupId>.<artifactId> 或 Go 的根目录）定义。
- 依赖方向必须指向稳定抽象。业务域模块（Domain/Application 层） 内的具体实现不得依赖其他具体实现，必须依赖抽象。基础设施层（Infrastructure/Adapter 层） 允许依赖外部具体类库（数据库驱动、HTTP Client、云厂商 SDK），但必须通过封装类隔离，严禁将外部库的类型（如 ResultSet、OkHttp Response）泄漏到上层业务代码中。
- 禁止循环依赖。使用 jQAssistant、ArchUnit 或 go mod graph 在 CI 阶段自动检查，发现循环依赖直接构建失败。
- 模块边界按业务能力（Bounded Context）划分，严禁按技术分层（如 Controller/Service/DAO）作为顶层模块拆分依据。
- 模块里所有东西都围绕同一个目标。
- 相关代码放在一起。

### 1.2 当前迭代范围内的 YAGNI
- 用不到的别做。
- 能不写的，直接删除。
- 避免不必要的对象复制或克隆。
- YAGNI 的执行边界限定为当前迭代（Sprint）的用户故事。对于架构演进所需的基础设施（如日志门面、配置中心客户端、链路追踪埋点），即使当前故事未显式要求，允许预先引入，但必须附带不超过 2 行注释说明其针对的已知技术风险。
- 架构演进所需的抽象（含接口）不受当前 Sprint 限制。

### 1.3 代码符合 KISS 原则
- 用简单的代码解决问题。
- 避免多层嵌套，提前返回。
- 函数避免长参数列表，不要超过6个参数。超过此数时，必须将参数封装为专有的 Request Object/Config Struct，封装后的对象不得包含业务逻辑，仅作为数据容器。
- 能用if-else解决的，不要用Strategy 模式。
- 优先可读性，不优先"看起来专业”。

### 1.4 代码符合单一职责原则
- 每个函数只做一件清楚的事。
- 多步操作时，主函数只做调度，把每一步拆成独立函数。
- 每个文件只放一类东西。
- 文件名说啥，文件里就放啥。

### 1.5 命名是设计原则
- 使用有意义的、描述性的名称。
- 变里名/函数名要精确说明它装的是什么/做什么。
- 遵循项目或语言的命名规范。
- 避免缩写和单字母变量（除非是约定俗成的，如循环中的 i）。
- 函数名里不要用 do/process/handle 这种空动词。
- 如果需要注释解释命名，先改名字。

### 1.6 代码组织
- 严禁为只有一个具体实现的内部模块创建接口。只有满足以下任一条件，才允许引入接口：a) 按照1.2，该模块存在至少 2 个明确的、将在同一版本内上线的不同实现；b) 接口必须定义在生产源码目录（src/main）中。如果它只有一个实现且仅用于测试 Mock，请在接口上增加 @Internal 或注释标明“仅为测试稳定性而抽取，禁止扩展”，但绝不能放到测试目录里去。
- 调用方只能依赖接口中声明的方法，不得对返回的具体类型做强制类型断言（除非该断言由框架保证）。
- 使用适当的并发控制机制。

### 1.7 注释与文档
- 注释应该解释为什么，而不是做什么。
- 为公共 API 提供清晰的文档。
- 更新注释以反映代码变化。

## 2. 基础交互
- 所有回答都使用中文表述。
- 如需提供代码，为关键逻辑和可能造成理解困难的部分添加简明的中文注释。

## 3. 重构
### 3.1 小步重构
- 每次只做一个小改动，然后测试。
- 频繁提交，保持代码随时可工作。

### 3.2 测试保障
- 重构前确保有足够的测试。
- 每次修改后运行测试，确保行为不变。

### 3.3 代码审查
- 重构后进行代码审查，确保质量。

## 4. 性能优化的准入与准出规则（强制）
- 准入条件：没有 Profiling 报告（火焰图/CPU 采样/内存分配采样） 或者 生产环境监控面板（P99 延迟/错误率） 作为证据，禁止提交任何以“性能优化”为目的的代码改动。日常编码只遵循“逻辑清晰”原则，不考虑微观性能。
- 优化后的单函数最大圈复杂度不得高于优化前的单函数最大圈复杂度，允许因合理拆分导致的总圈复杂度上升，但需在注释中说明拆分依据。
- 数据结构选型：仅允许在 Profiling 证明容器内元素数量 > 10^4 且操作频率 > 10^3/s 时，才可将线性容器（数组/List）改为哈希/树形容器。其余场景一律使用最直观的序列容器。
- 并发优化硬性红线：引入 Goroutine/Thread/协程前，必须明确标注该并发生命周期的取消（Cancel）机制和父协程的等待（Wait/Join）策略。无法在注释中写清楚取消链的，禁止并发。

## 5. 绝对禁止
- 未经确认，禁止安装/删除依赖、修改构建脚本（pom.xml/go.mod/package.json）、修改核心配置文件（application.yml 等）。
- 禁止自动执行 git push、git reset --hard、rm -rf 等破坏性或远程变更操作。
- 禁止将任何密码、Token、私钥、数据库连接串以明文形式出现在源码、配置仓库或前端静态资源中。必须使用密钥管理服务（Vault/KMS）或环境变量注入。
- 禁止在代码中提交 try { ... } catch (Exception e) {} （空捕获体）或 except: pass（无具体异常类型且无日志），无论任何理由。
- 禁止在日志中记录身份证、手机号、密码哈希值等敏感信息（脱敏后也不行，除非合规部门明确书面授权）。

## 6. 执行权限划分
- 自动执行：读取文件、代码搜索、格式化。
- 确认后执行：安装依赖、修改核心配置、删除文件、Git推送。
- 关于“自动执行测试”的权限：
a) 单元测试（无网络、无文件写入、无DB变更）可自动执行。
b) 集成测试/端到端测试（涉及外部依赖或写入临时表）必须设为“确认后执行”，且执行前必须输出即将变更的数据库表名和网络端点清单。

## 7. 异常处理规则
### 7.1 异常处理的三层强制规则
- 第一层（资源层）：任何获取了堆外资源（文件句柄、DB 连接、网络 Socket、锁）的代码块，无论是否捕获异常，必须紧挨着资源申请处使用 try-with-resources（Java）、defer（Go）、using（C#）、finally（JS/Python）释放资源。违反此条，Code Review 直接打回，不讨论。
- 第二层（业务层）：默认不捕获异常。仅在以下两类场景允许显式 try-catch：
a) 重试型异常（如超时、连接中断），捕获后必须实施带退避（Backoff）的重试，且总重试时长不超过业务 SLA 的 30%；
b) 转换型异常（如将底层 SQLException 转换为业务异常码），转换后必须通过日志记录原始异常堆栈（WARN 级别以上），转换型异常，记录 WARN 日志后，直接抛出自定义业务异常，由第三层的全局拦截器序列化返回给调用方。
- 第三层（边界层）：在 HTTP/RPC/消息队列的入口处，必须存在唯一的全局异常拦截器（中间件）。未被上述两层处理的异常，在此统一序列化为标准错误响应，并记录完整上下文（traceId + 入参快照）。严禁在拦截器中静默返回空对象或 200 OK。

### 7.2 异常处理的其他规则
- 在数据边界(API输入、DB输出)校验输入，出错立即抛具体异常。
- 报错信息要包含"是什么值导致的"。

### 8. 其他
- 需求不明先提问。
- 小步迭代，避免批量修改。

## 9. 疑问与矛盾处理
- 在执行项目过程中，如果有对于本规则有任何疑问或者发现任何矛盾的地方，及时把这些问题汇总并反馈，等待最终决定后再执行。

## 10. 优化与改进
- 在执行项目过程中，如果有更利于本项目执行和发展的建议、优化或者改进的意见，及时把这些建议与意见汇总并反馈，等待最终决定后再执行。

## 11. 本项目开发避坑指南
- 为了安全而且高效地进行本项目开发，请在开发过程中参考以下文件：
- "D:\5 Test\Solo\Dolphinquiz Web\原基础构建AGENTS.md"
- "D:\5 Test\Solo\Dolphinquiz Web\原基础构建CLAUDE.md"


